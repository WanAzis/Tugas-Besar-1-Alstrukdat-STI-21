#include <stdio.h>
#include "console.h"
#include <stdlib.h>

int main(){
    // FITURE();
    STARTBNMO(&QueueGame);
    QUEUEGAME(&QueueGame);
    QUEUEGAME(&QueueGame);
    QUEUEGAME(&QueueGame);
    PLAYGAME(&QueueGame);
}