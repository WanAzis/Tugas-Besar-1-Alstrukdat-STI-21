#include <stdio.h>
#include "implementasiadt.h"
#include "implementasiadt.c"

int main(){
    int score;
    // STARTWORD();
    // char kata[5];
    // WordToString(currentWord,kata);
    // printf("angkanya adalah : %i", atoi(kata));
    SnakeonMeteor(&score);
}